{
  "name": "luxio-monorepo",
  "private": true,
  "scripts": {
    "build": "node scripts/build-with-timer.mjs",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  }
}