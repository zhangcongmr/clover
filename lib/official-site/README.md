
  # Design UI from Snapshot

  This is a code bundle for Design UI from Snapshot. The original project is available at https://www.figma.com/design/QdEMVytUKNhW1SFHo6eSaK/Design-UI-from-Snapshot.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  